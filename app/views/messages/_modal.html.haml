.modal-overlay

-# 暗号
.encryption
  .encryption__text
    テキストとキーを入力してください
    （キーは大切に保管してください）
  = form_for [@group, @message], html:{id:'encryption_form',class:'encryption_form'} do |f|
    .encryption__form
    = f.text_field :content, class: 'encryption__form__text', placeholder: 'テキスト'
    = f.password_field :code, class: 'encryption__form__code', placeholder: 'キー'
    .encryption__btn
      = f.submit '実行する', class: 'encryption__btn__yes'
      .encryption__btn__no
        =link_to "#", class:"modal-btn" do
          閉じる

-# 解読
.decryption
  .decryption__text
    暗号とキーを入力してください
  = form_for [@group, @message], html:{id:'decryption_form',class:'decryption_form'} do |f|
    .decryption__form
    = f.text_field :content, class: 'decryption__form__text', placeholder: '暗号'
    = f.password_field :key, class: 'decryption__form__key', placeholder: 'キー'
    .decryption__btn
      = f.submit '解読する', class: 'decryption__btn__yes'
      .decryption__btn__no
        =link_to "#", class:"modal-btn" do
          閉じる